apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: restreamer
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/tools/restreamer/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster
