---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jelu
  namespace: jelu
spec:
  interval: 30m
  timeout: 25m
  maxHistory: 3
  # enabled | warn
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: app-template
      version: 18.16.9
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    gatus:
      group: media-books
    global:
      stopAll: false
    credentials:
      cf-restic:
        bucket: "${CF_S3_RESTIC_BUCKET}-jelu"
      rfs-restic:
        bucket: "${RUSTFS_S3_RESTIC_BUCKET}-jelu"
    image:
      repository: wabayang/jelu
      pullPolicy: IfNotPresent
      tag: 0.84.4@sha256:bddc4606c7247c253c1cd195d07c723f989e8c2feb1b4d9e282c245660ab6974
    securityContext:
      container:
        readOnlyRootFilesystem: false
        runAsGroup: 0
        runAsUser: 0
    service:
      main:
        ports:
          main:
            port: 11111
    ingress:
      main:
        enabled: true
        ingressClassName: "internal"
        hosts:
          - host: jelu.${DOMAIN_2}
            paths:
              - path: /
                pathType: Prefix
        integrations:
          nginx:
            enabled: true
          certManager:
            enabled: true
            certificateIssuer: general-le-prod
    workload:
      main:
        podSpec:
          containers:
            main:
              env:
                SERVER_PORT: "{{ .Values.service.main.ports.main.port }}"
                JELU_DATABASE_PATH: "{{.Values.persistence.db.mountPath }}"
                JELU_FILES_IMPORTS: "{{.Values.persistence.imports.mountPath }}"
                JELU_FILES_IMAGES: "{{.Values.persistence.images.mountPath }}"
                # JELU_SESSION_DURATION: 604800
                LANGUAGE: en_US:en
                # LDAP                
                # JELU_AUTH_LDAP_ENABLED=false
                # JELU_AUTH_LDAP_URL=ldap://127.0.0.1:389/dc=home,dc=lab
                # JELU_AUTH_LDAP_USERDN_PATTERNS_0=uid={0}
                # JELU_AUTH_LDAP_USERDN_PATTERNS_1=cn={0},ou=users
                # JELU_AUTH_LDAP_USERSEARCHFILTER=(cn={0})
                # JELU_AUTH_PROXY_ENABLED=false
                # JELU_AUTH_PROXY_ADMINNAME=adminproxy
                # JELU_METADATA_CALIBRE_PATH: /usr/bin/fetch-ebook-metadata
                # QTWEBENGINE_DISABLE_SANDBOX: 1
    persistence:
      config:
        enabled: true
        mountPath: /config
        volsync:
          - name: config
            type: restic
            credentials: cf-restic
            dest:
              enabled: true
            src:
              enabled: true
      db:
        enabled: true
        mountPath: /db
        volsync:
          - name: db
            type: restic
            credentials: cf-restic
            dest:
              enabled: true
            src:
              enabled: true
      images:
        enabled: true
        mountPath: /images
        volsync:
          - name: images
            type: restic
            credentials: cf-restic
            dest:
              enabled: true
            src:
              enabled: true
      imports:
        enabled: true
        mountPath: /imports
        volsync:
          - name: imports
            type: restic
            credentials: cf-restic
            dest:
              enabled: true
            src:
              enabled: true
    cnpg:
      main:
        enabled: false
