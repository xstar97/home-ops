### infrastructure
---
# cert-manager
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cert-manager
  namespace: grafana
spec:
  folderRef: infrastructure
  instanceSelector:
    matchLabels:
      dashboards: grafana
  grafanaCom:
    # renovate: depName="Cert-manager-Kubernetes"
    id: 20842
    revision: 3
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus

---
# cnpg dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cnpg
  namespace: grafana
spec:
  folderRef: infrastructure
  instanceSelector:
    matchLabels:
      dashboards: grafana
  grafanaCom:
    # renovate: depName="CloudNativePG"
    id: 20417
    revision: 4
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus

---
# longhorn dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: longhorn
  namespace: grafana
spec:
  folderRef: infrastructure
  instanceSelector:
    matchLabels:
      dashboards: grafana
  grafanaCom:
    # renovate: depName="Longhorn"
    id: 23521
    revision: 2
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus


### gitops
---
# flux-cluster dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: flux-cluster
  namespace: grafana
spec:
  folderRef: gitops
  instanceSelector:
    matchLabels:
      dashboards: grafana
  # renovate: depName="FluxCD"
  url: https://raw.githubusercontent.com/fluxcd/flux2-monitoring-example/refs/heads/main/monitoring/configs/dashboards/cluster.json
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus

---
# flux-control-plane dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: flux-control-plane
  namespace: grafana
spec:
  folderRef: gitops
  instanceSelector:
    matchLabels:
      dashboards: grafana
  # renovate: depName="FluxCD"
  url: https://raw.githubusercontent.com/fluxcd/flux2-monitoring-example/refs/heads/main/monitoring/configs/dashboards/control-plane.json
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus


### kubernetes
---
# node-feature-discovery dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: node-feature-discovery
  namespace: grafana
spec:
  folderRef: kubernetes
  instanceSelector:
    matchLabels:
      dashboards: grafana
  # renovate: depName="Node Feature Discovery" datasource=github-tags
  url: https://raw.githubusercontent.com/kubernetes-sigs/node-feature-discovery/refs/tags/v0.18.3/examples/grafana-dashboard.json
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus

---
# metallb dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: metallb
  namespace: grafana
spec:
  folderRef: kubernetes
  instanceSelector:
    matchLabels:
      dashboards: grafana
  grafanaCom:
    # renovate: depName="Metallb"
    id: 20162
    revision: 6
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus

---
# ingress-nginx dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: nginx
  namespace: grafana
spec:
  folderRef: kubernetes
  instanceSelector:
    matchLabels:
      dashboards: grafana
  # renovate: depName="ingress-nginx" datasource=github-tags
  url: https://github.com/kubernetes/ingress-nginx/raw/refs/tags/ingress-nginx-3.15.2/deploy/grafana/dashboards/nginx.json
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus

---
# cilium dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cilium
  namespace: grafana
spec:
  folderRef: kubernetes
  instanceSelector:
    matchLabels:
      dashboards: grafana
  grafanaCom:
    # renovate: depName="Cilium"
    id: 21431
    revision: 6
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus

---
# grafana kubernetes dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: kubernetes
  namespace: grafana
spec:
  folderRef: kubernetes
  instanceSelector:
    matchLabels:
      dashboards: grafana
  grafanaCom:
    # renovate: depName="k8s"
    id: 15661
    revision: 2
  datasources:
    - inputName: DS_VICTORIAMETRICS_PROD_ALL
      datasourceName: Prometheus

---
# node-exporter dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: node-exporter
  namespace: grafana
spec:
  folderRef: kubernetes
  instanceSelector:
    matchLabels:
      dashboards: grafana
  grafanaCom:
    # renovate: depName="Node Exporter Full"
    id: 1860
    revision: 45
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus


### storage
---
# volsync dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: volsync
  namespace: grafana
spec:
  folderRef: storage
  instanceSelector:
    matchLabels:
      dashboards: grafana
  grafanaCom:
    # renovate: depName="VolSync Dashboard"
    id: 21356
    revision: 3
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus


### media
---
# arrs dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: arrs
  namespace: grafana
spec:
  folderRef: media
  instanceSelector:
    matchLabels:
      dashboards: grafana
  # renovate: depName="Exportarr"
  url: https://raw.githubusercontent.com/onedr0p/exportarr/refs/heads/main/examples/grafana/dashboard2.json
  datasources:
    - inputName: DS_PROMETHEUS
      datasourceName: Prometheus
