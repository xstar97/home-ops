x-healthcheck: &healthcheck
  test: ["CMD-SHELL","wget -q --spider http://localhost:2376/_hawser/health || exit 1"]
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 15s

services:
  hawser:
    scale: 1
    image: ghcr.io/finsys/hawser:0.2.49@sha256:fbcff2d71eb3bb0c573d2861aae95d5f2b3e8124cae2532ea02673f90209fa29
    container_name: hawser
    restart: on-failure
    env_file:
      - hawser.enc.env
    environment:
      STACKS_DIR: "/opt/hawser-stacks"
    networks:
      - network_internal
    ports:
      - 2376:2376
    healthcheck: *healthcheck
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - hawser_data:/opt/hawser-stacks
networks:
  network_internal:
    external: true
  internal:
    driver: bridge
volumes:
  hawser_data: {}